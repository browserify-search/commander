#! /usr/bin/env node

var api = require('../lib/api')

api('droplets', function(err, reply){
  var droplets = reply.droplets
  var active = droplets.filter(function(d){
    return d.status === 'active'
  })

  console.log(active.length + ' out of ' + droplets.length + ' active')
})